import { ToolType } from "~/store/types/Global";

export const TOOLBAR_ACTIONS: ToolType[] = [ToolType.UNDO, ToolType.REDO];

export const TOOLBAR_TOOLS: ToolType[] = [
    ToolType.HAND,
    ToolType.SELECTION,
    ToolType.ZOOM,
    ToolType.TEXT,
    ToolType.IMAGE,
];
